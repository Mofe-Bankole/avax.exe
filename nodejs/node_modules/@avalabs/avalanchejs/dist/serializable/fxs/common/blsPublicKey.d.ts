import type { PublicKey } from '../../../crypto/bls';
import { TypeSymbols } from '../../constants';
import { Primitives } from '../../primitives/primatives';
export declare class BlsPublicKey extends Primitives {
    readonly publicKey: PublicKey;
    _type: TypeSymbols;
    constructor(publicKey: PublicKey);
    static fromPublicKeyBytes(publicKeyBytes: Uint8Array): BlsPublicKey;
    static fromBytes(bytes: Uint8Array): [blsPublicKey: BlsPublicKey, rest: Uint8Array];
    static fromHex(hex: string): BlsPublicKey;
    toBytes(): Uint8Array;
    toString(): string;
    toJSON(): string;
}
//# sourceMappingURL=blsPublicKey.d.ts.map