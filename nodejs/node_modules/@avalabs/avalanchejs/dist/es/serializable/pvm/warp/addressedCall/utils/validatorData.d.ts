import { BlsPublicKey, NodeId } from '../../../../fxs/common';
import type { Codec } from '../../../../codec';
import { TypeSymbols } from '../../../../constants';
import { BigIntPr } from '../../../../primitives';
export declare class ValidatorData {
    readonly nodeId: NodeId;
    readonly blsPublicKey: BlsPublicKey;
    readonly weight: BigIntPr;
    _type: TypeSymbols;
    constructor(nodeId: NodeId, blsPublicKey: BlsPublicKey, weight: BigIntPr);
    static fromBytes(bytes: Uint8Array, codec: Codec): [ValidatorData, Uint8Array];
    toBytes(codec: Codec): Uint8Array;
}
//# sourceMappingURL=validatorData.d.ts.map