import { Address, Id } from '../../../../fxs/common';
import type { Codec } from '../../../../codec';
import { TypeSymbols } from '../../../../constants';
import { ValidatorData } from './validatorData';
export declare class ConversionData {
    readonly subnetId: Id;
    readonly managerChainId: Id;
    readonly managerAddress: Address;
    readonly validators: ValidatorData[];
    _type: TypeSymbols;
    constructor(subnetId: Id, managerChainId: Id, managerAddress: Address, validators: ValidatorData[]);
    static fromBytes(bytes: Uint8Array, codec: Codec): [ConversionData, Uint8Array];
    toBytes(codec: Codec): Uint8Array;
}
//# sourceMappingURL=conversionData.d.ts.map