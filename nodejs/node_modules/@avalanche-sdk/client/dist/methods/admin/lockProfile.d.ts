import { Chain, Transport } from "viem";
import { AvalancheCoreClient as Client } from "../../clients/createAvalancheCoreClient.js";
/**
 * Writes a profile of mutex statistics to `lock.profile`.
 *
 * - Docs: https://build.avax.network/docs/api-reference/admin-api#adminlockprofile
 *
 * @param client - The client to use. {@link AvalancheCoreClient}
 * @returns Promise that resolves when the profile is written
 *
 * @example
 * ```ts
 * import { createAvalancheCoreClient } from '@avalanche-sdk/client'
 * import { avalanche } from '@avalanche-sdk/client/chains'
 * import { lockProfile } from '@avalanche-sdk/client/methods/admin'
 *
 * const client = createAvalancheCoreClient({
 *   chain: avalanche,
 *   transport: {
 *     type: "http",
 *     url: "<url>",
 *   },
 * });
 *
 * await lockProfile(client);
 * ```
 */
export declare function lockProfile<chain extends Chain | undefined>(client: Client<Transport, chain>): Promise<void>;
//# sourceMappingURL=lockProfile.d.ts.map