services:
  postgres:
    image: postgres:16
    container_name: avax_postgres_database
    restart: always
    environment:
      POSTGRES_USER: mofebanks
      POSTGRES_PASSWORD: avax0001001
      POSTGRES_DB: avax_postgres_database
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
