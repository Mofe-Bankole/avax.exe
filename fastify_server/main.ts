import { app } from "./server";
import logger from "./src/utils/logger";

// app.listen({ port: 4070 }, function (err, address) {
//     if (err) {
//       logger.info(`${err}`);
//     }
//   });
  